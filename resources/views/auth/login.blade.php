<!DOCTYPE html>
<html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Вход — Тихий спутник</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body class="min-h-screen bg-ink-950"><main class="grid min-h-screen place-items-center p-5"><section class="w-full max-w-md rounded-3xl border border-white/10 bg-white p-7 shadow-2xl md:p-9">
<p class="text-xs uppercase tracking-[0.24em] text-amber-500">Закрытое пространство команды</p><h1 class="mt-3">Тихий спутник</h1><p class="mt-2 text-sm text-slate-500">Управление подготовкой и производством фильма</p>
@if($errors->any())<div class="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif
<form method="POST" action="{{ route('login.store') }}" class="mt-7 space-y-4">@csrf<div><label for="email">{{ __('ui.auth.email') }}</label><input id="email" name="email" type="email" value="{{ old('email') }}" required autofocus autocomplete="email"></div><div><label for="password">{{ __('ui.auth.password') }}</label><input id="password" name="password" type="password" required autocomplete="current-password"></div><label class="flex items-center gap-2"><input class="h-4 w-4" type="checkbox" name="remember" value="1"><span class="text-sm">{{ __('ui.auth.remember') }}</span></label><button class="btn-primary w-full">{{ __('ui.auth.login') }}</button></form>
<p class="mt-6 text-xs leading-5 text-slate-400">{{ __('ui.auth.registration_disabled') }}</p></section></main></body></html>
