@extends('layouts.app')
@section('title', 'Материалы')
@section('content')
<div class="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p class="text-sm text-amber-500">Фото, видео, звук, документы и ссылки</p><h1>Материалы</h1></div><a href="{{ route('assets.create') }}" class="btn-primary">Загрузить материал</a></div>
<form method="GET" class="card mt-6 grid gap-3 md:grid-cols-4"><input name="q" value="{{ request('q') }}" placeholder="Поиск по названию"><select name="type"><option value="">Все типы</option>@foreach(\App\Models\Asset::TYPES as $type)<option @selected(request('type')===$type)>{{ $type }}</option>@endforeach</select><select name="status"><option value="">Все статусы</option>@foreach(\App\Models\Asset::STATUSES as $status)<option @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select><button class="btn-secondary">Применить фильтры</button></form>
<section class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">@forelse($assets as $asset)<a href="{{ route('assets.show',$asset) }}" class="card overflow-hidden p-0 transition hover:-translate-y-0.5 hover:border-amber-400">@if($asset->thumbnail_path)<img src="{{ route('assets.thumbnail',$asset) }}" alt="" class="h-44 w-full object-cover">@else<div class="grid h-28 place-items-center bg-ink-900 text-sm text-mist-200">{{ $asset->type }}</div>@endif<div class="p-4"><div class="flex justify-between gap-3"><h3>{{ $asset->title }}</h3><span class="badge bg-mist-100">{{ $asset->status }}</span></div><p class="mt-2 line-clamp-2 text-sm text-slate-500">{{ $asset->description }}</p><p class="mt-3 text-xs text-slate-400">{{ $asset->category?->name }} · {{ $asset->created_at->format('d.m.Y') }}</p></div></a>@empty<div class="card sm:col-span-2 xl:col-span-3"><p class="muted">По выбранным фильтрам материалов нет.</p></div>@endforelse</section><div class="mt-6">{{ $assets->links() }}</div>
@endsection
